machine:
  kubelet:
    extraConfig:
      maxPods: 200
    extraMounts:
      - destination: /var/openebs/local
        type: bind
        source: /var/openebs/local
        options:
          - bind
          - rshared
          - rw
    nodeIP:
      validSubnets:
        - 192.168.100.0/24
