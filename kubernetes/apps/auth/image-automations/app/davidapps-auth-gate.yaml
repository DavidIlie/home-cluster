---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: davidapps-auth-gate
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: davidapps-auth-gate
  filterTags:
    # release-206 cannot load the current production database snapshot. Keep
    # the last healthy gate pinned until its schema rollout is complete.
    pattern: "^release-(?P<number>203)$"
    extract: "$number"
  policy:
    numerical:
      order: asc
  digestReflectionPolicy: Always
  interval: 1m0s
---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImageRepository
metadata:
  name: davidapps-auth-gate
  namespace: flux-system
spec:
  image: ghcr.io/davidilie/davidapps-auth-gate
  interval: 1m0s
  secretRef:
    name: docker-regcred
