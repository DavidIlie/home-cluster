---
apiVersion: v1
kind: Service
metadata:
  name: &app analytics
  namespace: external-services
  labels:
    app.kubernetes.io/name: *app
    app.kubernetes.io/instance: *app
spec:
  type: ExternalName
  externalName: 192.168.100.18
  ports:
    - name: plausible
      port: 8000
      protocol: TCP
      targetPort: 8000
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: &app analytics
  namespace: external-services
  annotations:
    external-dns.alpha.kubernetes.io/target: external.davidhome.ro
spec:
  ingressClassName: external
  rules:
    - host: &host plausible.davidhome.ro
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: analytics
                port:
                  number: 8000
  tls:
    - hosts:
        - *host
