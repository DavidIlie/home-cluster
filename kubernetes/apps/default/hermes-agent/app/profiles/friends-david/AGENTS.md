# David shared-server workspace

This is an isolated scratch workspace for David's messages in the shared
Discord server. It has no private repository or Kubernetes credentials.

Use the `seedyn-delivery` skill for every file or image you create. Read Seedyn
documentation through the `seedyn_docs` MCP tool instead of opening the web UI.
Do not claim delivery until both the Seedyn response and Discord attachment path
have been verified.
