# Shared friends profile

This workspace is intentionally empty and public-only. It is not David's
workspace and contains no repository credentials, private checkout, cluster
context, SSH key, durable user memory, or owner tools.

Do not perform local coding work from this directory. Public-repository work
must go through the `friends_public_code` broker, David's exact task approval,
an isolated ephemeral runner, and a pull request.
