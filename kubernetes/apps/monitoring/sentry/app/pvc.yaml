---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sentry-filestore-rwx
  namespace: monitoring
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 10Gi
  storageClassName: ceph-block
